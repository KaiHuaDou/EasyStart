<Window x:Class="StartPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:StartPro"
        Width="512"
        Height="512"
        Closing="SaveTiles">
    <Window.Resources>
        <Style x:Key="IconButton" BasedOn="{StaticResource {x:Type Button}}" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily" Value="Segoe Fluent Icons" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="5,5,10,10" />
            <Setter Property="Width" Value="32" />
        </Style>
    </Window.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Style="{StaticResource IconButton}" Click="AddTile" Content="&#xE710;" />
            <Button Style="{StaticResource IconButton}" Content="&#xE74D;" />
        </StackPanel>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="mainGrid" Background="{StaticResource Background}" />
        </ScrollViewer>
    </DockPanel>
</Window>
